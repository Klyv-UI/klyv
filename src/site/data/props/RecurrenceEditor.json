{
  "props": [
    {
      "name": "value",
      "type": "RecurrenceEditorValue",
      "required": false,
      "description": "Controlled rule."
    },
    {
      "name": "defaultValue",
      "type": "Partial<RecurrenceEditorValue>",
      "required": false,
      "description": "Starting rule when uncontrolled. Missing fields take the defaults."
    },
    {
      "name": "onValueChange",
      "type": "(value: RecurrenceEditorValue, details: { summary: string; rrule: string }) => void",
      "required": false,
      "description": "Called with the rule, its sentence and its RRULE after every change."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "defaultValue": "'Repeats'",
      "description": "Names the group."
    },
    {
      "name": "showRRule",
      "type": "boolean",
      "required": false,
      "defaultValue": "false",
      "description": "Show the RRULE string under the summary."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
