{
  "props": [
    {
      "name": "name",
      "type": "string",
      "required": false,
      "defaultValue": "'klyv'",
      "description": "The election and channel name. Tabs with the same name coordinate."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "defaultValue": "'Open tabs'",
      "description": "Heading of the panel."
    },
    {
      "name": "onAcquire",
      "type": "() => void | (() => void)",
      "required": false,
      "description": "Runs when this tab becomes the leader; return a cleanup for when it stops."
    },
    {
      "name": "onLeaderChange",
      "type": "(isLeader: boolean) => void",
      "required": false,
      "description": "Called when this tab gains or loses the leadership."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
