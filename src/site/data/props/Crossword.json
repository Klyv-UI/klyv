{
  "props": [
    {
      "name": "puzzle",
      "type": "CrosswordPuzzle",
      "required": true
    },
    {
      "name": "onComplete",
      "type": "() => void",
      "required": false,
      "description": "Called once when every letter is correct."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "defaultValue": "'Crossword'",
      "description": "Accessible name for the grid."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
