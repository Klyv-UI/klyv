{
  "props": [
    {
      "name": "keys",
      "type": "string[]",
      "required": false,
      "description": "Only show these parameters. Defaults to all of them."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "defaultValue": "'Query string'",
      "description": "Heading above the readout."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
