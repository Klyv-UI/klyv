{
  "props": [
    {
      "name": "panels",
      "type": "Record<string, DockLayoutPanel>",
      "required": true,
      "description": "Every panel that can appear, by id. The layout refers to them by these ids."
    },
    {
      "name": "value",
      "type": "DockLayoutNode",
      "required": false,
      "description": "Controlled layout tree."
    },
    {
      "name": "defaultValue",
      "type": "DockLayoutNode",
      "required": false,
      "description": "Starting layout when uncontrolled."
    },
    {
      "name": "onValueChange",
      "type": "(layout: DockLayoutNode) => void",
      "required": false,
      "description": "Called with the new tree after every move, resize, close or tab switch."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "defaultValue": "'Workspace'",
      "description": "Accessible name for the whole workspace."
    },
    {
      "name": "height",
      "type": "number",
      "required": false,
      "defaultValue": "420",
      "description": "Height of the workspace, in pixels."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
