{
  "props": [
    {
      "name": "name",
      "type": "string",
      "required": true,
      "description": "Full name — drives the initials, the tint and the accessible name."
    },
    {
      "name": "src",
      "type": "string",
      "required": false
    },
    {
      "name": "size",
      "type": "AvatarSize",
      "required": false,
      "defaultValue": "'sm'"
    },
    {
      "name": "ring",
      "type": "boolean",
      "required": false,
      "defaultValue": "false",
      "description": "White halo, for avatars sitting on a busy surface."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
