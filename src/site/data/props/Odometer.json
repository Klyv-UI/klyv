{
  "props": [
    {
      "name": "value",
      "type": "number",
      "required": true,
      "description": "Value to display. Digits roll to their new position when it changes."
    },
    {
      "name": "decimals",
      "type": "number",
      "required": false,
      "defaultValue": "0",
      "description": "Fixed decimal places."
    },
    {
      "name": "prefix",
      "type": "string",
      "required": false,
      "description": "Prefix such as a currency symbol."
    },
    {
      "name": "size",
      "type": "OdometerSize",
      "required": false,
      "defaultValue": "'md'",
      "description": "Digit size, which sets both the type and the roll height."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "description": "Accessible text. Defaults to the formatted value."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
