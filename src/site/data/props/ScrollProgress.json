{
  "props": [
    {
      "name": "target",
      "type": "RefObject<HTMLElement | null>",
      "required": false,
      "description": "The scrolling element. Omit to track the window."
    },
    {
      "name": "variant",
      "type": "ScrollProgressVariant",
      "required": false
    },
    {
      "name": "size",
      "type": "number",
      "required": false,
      "description": "Bar thickness, or ring diameter, in pixels."
    },
    {
      "name": "fixed",
      "type": "boolean",
      "required": false,
      "description": "Pin to the viewport. Turn it off to place it inside a container."
    },
    {
      "name": "position",
      "type": "'top' | 'bottom'",
      "required": false,
      "description": "Which edge it pins to when fixed."
    },
    {
      "name": "showValue",
      "type": "boolean",
      "required": false,
      "description": "Show the percentage inside the ring."
    },
    {
      "name": "backToTop",
      "type": "boolean",
      "required": false,
      "defaultValue": "false",
      "description": "Render the ring as a back-to-top button. @deprecated Use `<BackToTop showProgress />`, which this now renders: it moves focus to the top of the page, jumps instead of animating under reduced motion, and leaves the tab order while hidden. `target`, `fixed` and `className` are passed on; the ring's size, colours, `showValue` and `hideUntil` are not."
    },
    {
      "name": "hideUntil",
      "type": "number",
      "required": false,
      "description": "Hide the ring until there is something to show."
    },
    {
      "name": "color",
      "type": "string",
      "required": false,
      "description": "Any CSS colour for the filled portion. Defaults to the accent."
    },
    {
      "name": "trackColor",
      "type": "string",
      "required": false,
      "description": "Any CSS colour for the portion not yet read."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
