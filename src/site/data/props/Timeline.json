{
  "props": [
    {
      "name": "items",
      "type": "TimelineItem[]",
      "required": true
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "description": "Accessible name for the sequence."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
