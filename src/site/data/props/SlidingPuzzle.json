{
  "props": [
    {
      "name": "size",
      "type": "number",
      "required": false,
      "defaultValue": "4",
      "description": "Tiles per side. 4 is the classic fifteen puzzle."
    },
    {
      "name": "imageTiles",
      "type": "boolean",
      "required": false,
      "defaultValue": "false",
      "description": "Paint a generated picture across the tiles instead of plain numbers. The numbers stay, small, as a guide."
    },
    {
      "name": "onSolve",
      "type": "(result: SlidingPuzzleResult) => void",
      "required": false,
      "description": "Called when the tiles are back in order."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "defaultValue": "'Sliding puzzle'",
      "description": "Accessible name for the board."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
