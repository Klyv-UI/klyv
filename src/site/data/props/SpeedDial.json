{
  "props": [
    {
      "name": "actions",
      "type": "SpeedDialAction[]",
      "required": true,
      "description": "The secondary actions, nearest the trigger first."
    },
    {
      "name": "label",
      "type": "string",
      "required": true,
      "description": "Accessible name for the trigger — \"Create\"."
    },
    {
      "name": "icon",
      "type": "IconComponent",
      "required": false,
      "description": "Trigger glyph. Rotated an eighth of a turn while open, so a plus becomes a cross."
    },
    {
      "name": "direction",
      "type": "SpeedDialDirection",
      "required": false,
      "defaultValue": "'up'",
      "description": "Which way the actions fan out from the trigger."
    },
    {
      "name": "labels",
      "type": "'visible' | 'hover'",
      "required": false,
      "defaultValue": "'visible'",
      "description": "`visible` prints each label beside its action; `hover` shows it on hover and focus only."
    },
    {
      "name": "open",
      "type": "boolean",
      "required": false,
      "description": "Controlled open state."
    },
    {
      "name": "defaultOpen",
      "type": "boolean",
      "required": false,
      "defaultValue": "false",
      "description": "Starting state when uncontrolled."
    },
    {
      "name": "onOpenChange",
      "type": "(open: boolean) => void",
      "required": false,
      "description": "Called when it opens or closes."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged onto the root. Position it here — `fixed bottom-6 right-6`."
    }
  ]
}
