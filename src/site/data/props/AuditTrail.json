{
  "props": [
    {
      "name": "entries",
      "type": "AuditEntry[]",
      "required": true
    },
    {
      "name": "label",
      "type": "string",
      "required": true,
      "description": "Accessible name for the list."
    },
    {
      "name": "actorFilter",
      "type": "string",
      "required": false,
      "description": "Show only entries by this actor name."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
