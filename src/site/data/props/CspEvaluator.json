{
  "props": [
    {
      "name": "value",
      "type": "string",
      "required": false,
      "description": "Controlled policy text: the header value, with or without the header name."
    },
    {
      "name": "defaultValue",
      "type": "string",
      "required": false,
      "defaultValue": "''",
      "description": "Starting policy when uncontrolled."
    },
    {
      "name": "onValueChange",
      "type": "(value: string) => void",
      "required": false,
      "description": "Called with the policy after every edit."
    },
    {
      "name": "onReport",
      "type": "(report: CspEvaluatorReport) => void",
      "required": false,
      "description": "Called with the review whenever the policy changes."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "defaultValue": "'Content-Security-Policy'",
      "description": "Visible label for the policy field."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
