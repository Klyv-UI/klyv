{
  "props": [
    {
      "name": "previous",
      "type": "ArticlePagerLink",
      "required": false,
      "description": "The article before this one. Omit on the first page."
    },
    {
      "name": "next",
      "type": "ArticlePagerLink",
      "required": false,
      "description": "The article after this one. Omit on the last page."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "defaultValue": "'Previous and next article'",
      "description": "Name of the navigation landmark, so it is distinct from the site's other navs."
    },
    {
      "name": "linkAs",
      "type": "ElementType",
      "required": false,
      "description": "Link element — a router link that takes `href`. Defaults to `a`."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Merged last, so it wins."
    }
  ]
}
